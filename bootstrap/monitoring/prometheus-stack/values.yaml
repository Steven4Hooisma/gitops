grafana:
  enabled: true
  adminPassword: "admin"
  service:
    type: ClusterIP

prometheus:
  prometheusSpec:
    serviceMonitorSelector: {}
    podMonitorSelector: {}

alertmanager:
  enabled: true
