apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: niek-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/configuration-snippet: |
      add_header X-Custom-Header "HelloWorld";
spec:
  rules:
  - host: niek.taggrs.dev
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: niek-dashboard-service
            port:
              number: 80
